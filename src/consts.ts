// Place any global data in this file.
// You can import this data from anywhere in your site by using the `import` keyword.

export const SITE_TITLE = 'Quantagram';
export const SITE_DESCRIPTION = 'Exploring the intersection of Quantum Computing and Artificial Intelligence.';

// Contact address shown on the privacy/contact page.
export const CONTACT_EMAIL = 'hello@quantagram.org';

// Show the dismissible cookie notice bar. Set to false to hide it (e.g. if you
// later rely solely on Google's Privacy & messaging CMP for EEA consent).
export const COOKIE_NOTICE = false;

// ── Google AdSense ───────────────────────────────────────────────────────────
// Leave empty to keep ads and the AdSense loader completely disabled.
// When ready, set this to your publisher ID, e.g. 'ca-pub-1234567890123456'.
export const ADSENSE_CLIENT = 'ca-pub-3899211853644288';
// Whether ad units render. Ads only appear when BOTH ADSENSE_CLIENT is set
// AND this is true — lets you load the script (for verification) before
// turning on live ad slots.
export const ADSENSE_ENABLED = false;

// ── Newsletter ───────────────────────────────────────────────────────────────
// Form POST endpoint from your email provider (Buttondown, Mailchimp, ConvertKit,
// Substack, etc.). While empty, inline newsletter CTAs link to the /newsletter
// page instead of submitting directly.
export const NEWSLETTER_ACTION = '';

// ── Comments (giscus — https://giscus.app) ───────────────────────────────────
// Enable GitHub Discussions on your repo, install the giscus app, then paste the
// values giscus.app generates. Leave GISCUS_REPO empty to disable comments.
export const GISCUS_REPO = 'kheops212/quantagram';
export const GISCUS_REPO_ID = 'R_kgDOTefcNg';
export const GISCUS_CATEGORY = 'Announcements';
export const GISCUS_CATEGORY_ID = 'DIC_kwDOTefcNs4DBm9y';
